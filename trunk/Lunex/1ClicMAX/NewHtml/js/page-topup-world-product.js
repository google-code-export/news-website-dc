$(function() {
	form.mask.setupMaskMany([
		
	]);
	form.validation.setupMany([
		
	]);

	
	// TODO: Execute page-scope functions here
});

if (!pages) {
	var pages = {};	
}
pages = $.extend(pages, {
	topUpWorld: {
		
	}	
});